import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
    backgroundColor: "lightgray",
    padding: 15,
    marginHorizontal: 10,
    borderRadius: 5,
  },

  iconContainer: {
    marginRight: 10,
  },
});
