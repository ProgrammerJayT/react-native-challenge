import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
    backgroundColor: "lightgray",
    padding: 20,
    marginHorizontal: 10,
    borderRadius: 5,
  },

  name: {
    fontSize: 20,
  },
});
